cmake_minimum_required(VERSION 2.8)
project(eye-detect)
find_package(OpenCV REQUIRED)
add_executable(eye-detect src/calibrate.cpp src/NeuralNetwork.cpp src/NeuralNetwork.h)
target_link_libraries(eye-detect ${OpenCV_LIBS})
